access=public
# Provenance is added explicitly by the CI release workflow (--provenance flag).
# Local publishes don't have an OIDC token issuer and would fail with:
#   "Automatic provenance generation not supported for provider: null"
